return {
  {
    "ahmedkhalf/project.nvim",
    opts = function(_, opts)
      opts.manual_mode = true
    end,
    -- config = function()
    --   require("project_nvim").setup({
    --     manual_mode = true,
    --   })
    -- end,
  },
}
